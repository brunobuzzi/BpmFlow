other
setUser: username group: groupname credentials: credentials pageSize: pSize pageNumber: pNumber xmlQuery: xmlDocument

	user := username.
	group := groupname.
	userCredentials := credentials.
	pageSize := pSize.
	pageNumber := pNumber.
	xmlQuery := xmlDocument