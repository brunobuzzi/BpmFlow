process execution
cancelSendTask
	"The receiver cancel the attached send task"

	"It does nothing because the send attempt was executed and failed.
	<BpmSendTaskInstance> is already 'cancelled' (not cancelled per se but executed) when reached this point"